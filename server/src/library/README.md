# library

Shared **library/tools** domain: utilities, helpers, and low-level tools used across the server.

- **tools**: pure helpers (logger, env, formatting, etc.). No business logic, no HTTP, no persistence.
