# endpoints

**Endpoints** domain: HTTP layer (Express routes and responses).

- Call **services** for business logic.
- Use **library/tools** if needed (e.g. logging).
- No business logic here – only request/response mapping.
